{
  "name": "dealer-rentcar",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicación web de renta de carros (localStorage + roles)",
  "scripts": {
    "serve": "python -m http.server 8000",
    "check": "node --check app.js"
  }
}
